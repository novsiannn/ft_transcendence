export interface IUserDataRegistrationType {
    text: string | null,
    email: string | null,
    password: string | null
}

export interface IUserDataTypeLogin {
    email: string | null,
    password: string | null
}
